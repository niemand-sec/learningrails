<%= form_for(@vulnerabilidad) do |f| %>
  <% if @vulnerabilidad.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vulnerabilidad.errors.count, "error") %> prohibited this vulnerabilidad from being saved:</h2>

      <ul>
      <% @vulnerabilidad.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :descripcion %><br>
    <%= f.text_field :descripcion %>
  </div>
  <div class="field">
    <%= f.label :fechaDescubrimiento %><br>
    <%= f.date_select :fechaDescubrimiento %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
