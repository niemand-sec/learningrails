<h1>Listing vulnerabilidad_version_sos</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @vulnerabilidad_version_sos.each do |vulnerabilidad_version_so| %>
      <tr>
        <td><%= link_to 'Show', vulnerabilidad_version_so %></td>
        <td><%= link_to 'Edit', edit_vulnerabilidad_version_so_path(vulnerabilidad_version_so) %></td>
        <td><%= link_to 'Destroy', vulnerabilidad_version_so, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Vulnerabilidad version so', new_vulnerabilidad_version_so_path %>
